# /figma - Implement Figma Design

1. Fetch Figma component via MCP (mcp__figma__* tools)
2. Extract: layout, spacing, colors (map to theme tokens), typography, icons
3. Implement using Tailwind v4 + shadcn/ui + cn()
4. Never hardcode hex colors - map to CSS variables from src/index.css
5. Respect 400px popup constraint
6. Run quality checks

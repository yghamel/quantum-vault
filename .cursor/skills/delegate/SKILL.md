# /delegate - Plan for Fast Models

Generate self-contained implementation plan for Sonnet/Haiku with:
1. Task summary (what + why)
2. File inventory (MODIFY/CREATE with exact paths)
3. Numbered steps with exact code, file paths, and reasoning
4. Validation commands (pnpm l:c && pnpm p:c)
5. P11 constraints reminder
